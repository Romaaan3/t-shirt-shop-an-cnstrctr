<div class="uk-container">
  <%= simple_form_for [:cp, @cp_product], html: { multipart: true, :class => 'uk-form' } do |f| %>
  <% if @cp_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cp_product.errors.count, "error") %> prohibited this cp_product from being saved:</h2>

      <ul>
      <% @cp_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="uk-form-row">
    <%= f.label "Имя" %><br>
    <%= f.text_field :name %>
  </div>

  <div class="uk-form-row">
    <%= f.label "Описание" %><br>
    <%= f.text_area :description %>
  </div>

  <div class="uk-form-row">
    <%= f.label "Фото" %><br>
    <%= f.file_field :photo %>
  </div>
  <div class="uk-form-row">
    <div class="actions">
    <%= f.submit %>
  </div>
  </div>

<% end %>

</div>
